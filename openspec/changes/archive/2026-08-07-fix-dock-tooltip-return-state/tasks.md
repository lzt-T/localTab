## 1. Dock 提示框状态控制

- [x] 1.1 在 `DockLinkItem` 内增加局部受控 Tooltip 状态与不触发渲染的点击抑制标记。
- [x] 1.2 在指针激活链接时关闭并抑制提示框，在指针离开链接时解除抑制。
- [x] 1.3 将抑制处理接入不会覆盖 dnd-kit 监听器的指针事件，并保持现有点击、键盘和拖拽行为。

## 2. 标签页恢复行为修正

- [x] 2.1 将提示框抑制标记调整为可驱动渲染的局部状态，并统一控制 Tooltip、浅色背景和图标位移反馈。
- [x] 2.2 根据页面可见性和指针转换目标区分真实页面内移动与标签页切换事件，仅在真实离开或重新进入时解除抑制。
- [x] 2.3 清除指针点击遗留的按钮焦点，同时保持键盘焦点语义和 dnd-kit 拖拽监听不变。

## 3. 交互验收

- [x] 3.1 手动确认点击 Dock 链接并切回原标签页时，提示框、浅色背景和图标位移均保持关闭。
- [x] 3.2 手动确认指针在可见页面内离开后再次悬停，或从页面其他位置重新进入链接时，完整悬停反馈正常恢复。
- [x] 3.3 手动确认 Dock 链接的打开、键盘焦点操作和拖拽排序未受影响。
