## 1. 本地领域服务

- [x] 1.1 调整分类创建服务，使其在保持现有副作用和调用兼容性的同时返回新建分类对象。
- [x] 1.2 为分类项目服务增加按分类范围和完整 URL 检查重复网址的能力，覆盖分类直属网址和文件夹内网址。

## 2. 快速收藏流程

- [x] 2.1 创建弹窗快速收藏 Hook，加载活动标签页和分类，限制 HTTP(S) 地址，并提供标题与 favicon 回退。
- [x] 2.2 在 Hook 中实现目标分类选择、名称校验、默认图标分类创建和创建后自动选择。
- [x] 2.3 在 Hook 中实现提交期间禁用、同分类重复拦截、分类末尾保存以及重复或失败反馈。

## 3. 弹窗界面与文案

- [x] 3.1 扩展 PopupApp，展示当前页面预览、分类选择器、快速创建分类输入和主要收藏操作，并保留现有次要入口。
- [x] 3.2 为当前页面状态、分类选择与创建、收藏结果和错误状态补充简体中文及英文文案。

## 4. 浏览器清单

- [x] 4.1 在 Chrome、Edge 和 Opera 扩展清单中加入 `activeTab` 最小权限，保持 Brave 复用 Chrome 清单的现有构建方式。

## 5. Popup 完成行为

- [x] 5.1 收藏成功后展示成功反馈并保持提交禁用，在 1 秒后关闭扩展弹窗。
- [x] 5.2 为 popup 页面解除全局 `body` 和 `#root` 的视口最小高度限制，并保持尺寸变化期间的深色背景一致。
