## 1. 浏览器权限与读取能力

- [x] 1.1 在 Chrome、Edge、Opera 清单中声明可选 `bookmarks` 权限，并确认 Brave 继续复用 Chrome 清单
- [x] 1.2 定义书签节点与权限状态所需的最小 Chromium API 类型
- [x] 1.3 实现只读浏览器书签服务，覆盖能力检测、用户手势授权、完整树读取和读取后释放权限
- [x] 1.4 修正可选权限检测顺序：授权前仅检查权限 API，授权成功后重新获取并检查 `bookmarks.getTree`

## 2. 书签树转换

- [x] 2.1 定义包含分类、文件夹、网址和跳过统计的导入计划类型
- [x] 2.2 实现 HTTP(S) 校验、标题回退和分类范围内完整 URL 去重
- [x] 2.3 实现顶层容器到分类、直属网址和嵌套路径到文件夹的有序转换
- [x] 2.4 实现空目录过滤、分类重名后缀和展平文件夹重名处理

## 3. 原子追加持久化

- [x] 3.1 为数据库工具增加跨 category、link、linkGroup 表的原子批量追加方法，使用新增语义避免覆盖现有 ID
- [x] 3.2 增加导入服务以读取现有分类上下文、构建导入计划并在用户确认后提交批量事务
- [x] 3.3 在事务成功后刷新分类数据并返回实际导入统计，在事务失败时保留导入前数据

## 4. 数据管理界面

- [x] 4.1 在数据管理中新增独立的浏览器书签导入区域，展示可选权限和只读用途说明
- [x] 4.2 实现权限请求、读取、预览、导入、完成和错误状态的导入 Hook 与状态分发
- [x] 4.3 实现导入预览界面，展示分类名称、分类/文件夹/网址数量、跳过统计以及确认和取消操作
- [x] 4.4 处理拒绝授权、API 不可用、读取失败、无有效网址、用户取消和写入失败反馈，确保不影响现有 JSON 数据管理

## 5. 双语内容与使用说明

- [x] 5.1 补充简体中文和英文的授权说明、导入状态、预览统计及结果反馈文案
- [x] 5.2 更新操作指南，说明浏览器书签为一次性只读导入且不会修改浏览器原书签
