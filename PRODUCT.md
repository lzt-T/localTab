# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

主要用户是希望高效整理个人常用网站的 Chromium 浏览器用户。他们在打开新标签页时，需要快速搜索、访问常用网站，并以符合个人习惯的方式维护网址分类、文件夹和固定入口。

## Product Purpose

LocalTab 将浏览器新标签页变成一个可由用户自行整理的本地网址工作区。用户可以集中管理常用网站、切换分类、搜索信息并调整页面背景。产品成功意味着用户能在每次打开新标签页时，更快找到并访问所需内容，同时保有对数据和布局的控制权。

## Positioning

LocalTab 以本地优先、无需账户的方式，把网址分类、文件夹、Dock、搜索和个性化背景集中在一个 Chromium 新标签页中。产品不依赖开发者服务器处理或收集用户数据，配置可由用户自行导出和恢复。

## Operating Context

- 产品以 Manifest V3 浏览器扩展运行，覆盖 Chrome、Edge、Opera 和 Brave。
- 用户主要在新标签页中浏览分类、打开网址、搜索、拖拽排序，以及创建或维护分类、网址和文件夹。
- 常用网址可固定到 Dock；分类、网址、文件夹和 Dock 项目支持拖拽管理。
- 设置面板用于更换背景、查看操作指南、管理备份和切换语言。
- 用户可以使用浏览器默认搜索提供商，或配置包含 `%s` 占位符的自定义搜索地址。

## Capabilities and Constraints

- 支持创建、编辑、删除和排序分类、快捷链接及网址文件夹。
- 支持在分类之间移动网址、合并网址为文件夹，以及将网址固定到 Dock。
- 支持网站 favicon、Lucide 图标和用户上传的自定义网址图标。
- 支持上传自定义背景图片。
- 支持简体中文、英文以及跟随浏览器语言。
- 支持将分类、网址、文件夹、搜索设置、Dock 和背景图片导出为 JSON，并从本地 JSON 恢复。
- 主要业务数据存储在浏览器 IndexedDB 中；部分设置可能使用浏览器提供的同步存储。
- 扩展权限限定为核心功能所需的 `storage`、`contextMenus`、`search` 和 `favicon`。
- 搜索和外部网址由用户主动触发，并在目标网站或搜索引擎中打开。

## Brand Commitments

- 产品名称为 LocalTab。
- 保留现有扩展图标资产：`public/icons/icon.svg` 以及对应的 PNG 尺寸文件。
- 产品文案保持简洁、直接、实用，现有界面同时提供简体中文和英文。
- 保留本地优先、无分析、无广告、无遥测、无用户跟踪的隐私承诺。

## Evidence on Hand

- `README.md`：产品定位、功能清单、支持浏览器、使用方式和技术栈。
- `PRIVACY.md`：本地数据处理、权限用途、外部搜索和数据导入导出承诺。
- `manifests/*.json`：扩展名称、版本、权限、入口和 Manifest V3 配置。
- `src/i18n/resources.ts`：当前简体中文和英文界面文案。
- `src/newtab/`：新标签页主要交互与功能实现。
- `public/icons/`：现有品牌图标资产。
- 当前没有可用于宣传的客户案例、用户评价、媒体报道或量化效果数据；未来工作不得虚构这些证据。

## Product Principles

1. 让高频访问路径保持快速、清晰且可由用户掌控。
2. 以本地优先和最小权限保护用户数据，不引入不必要的数据收集。
3. 让分类、文件夹、Dock 和搜索形成一致、可理解的网址组织方式。
4. 保证备份、恢复和跨浏览器使用不会破坏用户已整理的数据。
5. 保持核心功能对简体中文和英文用户一致可用。
